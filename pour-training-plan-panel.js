import { readBrewHistory } from './pour-brew-history.js';
import { chooseTrainingPlan, trainingPlanSummary } from './pour-training-plan.js';
function ensureStyle(doc){if(doc.getElementById('pourTrainingPlanStyle'))return;const s=doc.createElement('style');s.id='pourTrainingPlanStyle';s.textContent='.training-plan{display:none;margin-top:8px;padding:10px;border:1px solid var(--line);border-radius:12px;background:#0003}.training-plan.show{display:block}.training-plan-head{display:flex;justify-content:space-between;gap:8px;font-size:10px}.training-plan-body{display:grid;gap:4px;margin-top:6px;font-size:9px;color:var(--muted)}.training-plan-goal{color:var(--good);font-weight:700}';doc.head.appendChild(s)}
function ensurePanel(doc){let p=doc.getElementById('trainingPlan');if(p)return p;const trend=doc.getElementById('brewTrend');if(!trend?.parentElement)return null;p=doc.createElement('div');p.id='trainingPlan';p.className='training-plan';trend.insertAdjacentElement('beforebegin',p);return p}
export function renderTrainingPlan(doc=globalThis.document,storage=globalThis.localStorage){const p=ensurePanel(doc);if(!p)return {rendered:false};const plan=chooseTrainingPlan(readBrewHistory(storage),{window:8});p.replaceChildren();if(!plan.valid){p.classList.remove('show');return {rendered:false,plan}}const h=doc.createElement('div');h.className='training-plan-head';h.innerHTML=`<span>NEXT SESSION</span><b>${plan.mode==='focus'?plan.title:'综合稳定训练'}</b>`;p.appendChild(h);const body=doc.createElement('div');body.className='training-plan-body';for(const [i,line] of trainingPlanSummary(plan).entries()){const d=doc.createElement('div');if(i===1&&plan.mode==='focus')d.className='training-plan-goal';d.textContent=line;body.appendChild(d)}p.appendChild(body);p.classList.add('show');return {rendered:true,plan}}
export function installTrainingPlan(doc=globalThis.document,storage=globalThis.localStorage){if(!doc||doc.__pourTrainingPlanInstalled)return {installed:false};doc.__pourTrainingPlanInstalled=true;ensureStyle(doc);const update=()=>renderTrainingPlan(doc,storage);const results=doc.getElementById('results');if(results&&typeof MutationObserver!=='undefined'){const observer=new MutationObserver(update);observer.observe(results,{attributes:true,attributeFilter:['class']})}update();return {installed:true,update}}
