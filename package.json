{
  "name": "gallery-portfolio",
  "version": "1.0.0",
  "description": "Gallery Portfolio - 从Cloudflare R2生成图片索引",
  "main": "generate-gallery-index-r2.js",
  "scripts": {
    "generate-index": "node generate-gallery-index-r2.js",
    "deploy": "npm run generate-index && wrangler pages deploy . --project-name gallery-portfolio-static"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "keywords": [
    "gallery",
    "portfolio",
    "cloudflare",
    "r2",
    "static-site"
  ],
  "author": "Power",
  "license": "MIT"
}
