{
  "name": "gallery-portfolio-2",
  "version": "1.0.0",
  "description": "1. 将 `.env_template` 改名为 `.env`，并修改其中的配置\r 2. 使用以下方法部署：",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.582.0",
    "@aws-sdk/lib-storage": "^3.582.0",
    "aws-sdk": "^2.1626.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "sharp": "^0.33.4"
  }
}
